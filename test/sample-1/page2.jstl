---
title: Another JSTL Page
diff: {x: 1, y: 2}
layout: Whatever
---

<h1>${pkg.name}</h1>
${'ahoy' /*<${Layout} ...${layout_attrs}>
    <div ...${obj}>${
        array.map(item => html`<span>${item}</span>`)
    }</div>
<//>

<${Simple} ...${layout_attrs}>
    <div ...${obj}>${
        array.map(item => html`<span>${item}</span>`)
    }</div>
<//>*/}
